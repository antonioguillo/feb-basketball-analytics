import React from 'react';

const Loading = () => {
  return (
    <div style={{ textAlign: 'center', padding: '40px', color: '#8b949e' }}>
      <span>Cargando datos del scouting...</span>
    </div>
  );
};

export default Loading;
